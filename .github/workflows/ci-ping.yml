name: CI Ping

on:
  push:
    branches:
      - dev
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-22.04
    steps:
      - name: Echo
        run: |
          echo "CI ping on $(date -u)"
          echo "sha=${GITHUB_SHA}"
